doctype html
html(lang="en")
  head  
    title= siteName
  body
    include includes/header.pug
    if username
      h2 Welcome, #{username}!
      form(action='/goodbye', method='POST')
         button.btn.btn-primary(type='submit') goodbye
    block content 
    include includes/footer.pug